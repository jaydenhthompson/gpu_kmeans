#include "argparse.h"
#include "kmeans.h"
#include "seq_k_means.h"

#include <iostream>
#include <fstream>

int main(int argc, char **argv)
{
    options_t opts;
    get_opts(argc, argv, opts);

    if (opts.run_sequential)
    {

    }
    else
    {
    }
}